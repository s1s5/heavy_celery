============
Installation
============

At the command line::

    $ easy_install heavy_celery

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv heavy_celery
    $ pip install heavy_celery
